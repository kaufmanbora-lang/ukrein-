{
  "name": "shield-ukraine-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node -e \"console.log('No build step needed')\"",
    "check": "node --check app.js && node --check server.mjs && node --check env.mjs && node --check scripts/server.mjs && node --check scripts/write-config.mjs && node --check scripts/env.mjs",
    "dev": "node server.mjs",
    "start": "node server.mjs"
  }
}
